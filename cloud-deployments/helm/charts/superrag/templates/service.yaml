apiVersion: v1
kind: Service
metadata:
  name: {{ include "superrag.fullname" . }}
  labels:
    {{- include "superrag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superrag.selectorLabels" . | nindent 4 }}
