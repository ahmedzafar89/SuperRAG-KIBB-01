apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "superrag.labels" . | nindent 4 }}
  name: {{ include "superrag.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}